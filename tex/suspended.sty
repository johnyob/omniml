%% Fixing useless warning in acm mode
\AtBeginDocument
 {\let \@vspace@acm \@vspace@orig
  \let \@vspacer@acm \@vspacer@orig
  \let \@Description@presentfalse \relax
  }

%% These could be defaults, but we actually  enforce their
%% explicit definition in  \jobname.cfg

%% \providecommand \Draft {\False}
%% \providecommand \Final {\True}
%% \providecommand \Long {\True}
%% \providecommand \Anonymous {\False}

%% These are derived flags:

\newcommand{\Not}{\BNot}
\newcommand \Short {\Not\Long}

\RequirePackage{reversion}
\RequirePackage[utf8]{inputenc}
\RequirePackage{locallabel}

%% Enumerations

\RequirePackage[inline,shortlabels]{enumitem}
%% \setlist[enumerate]{label*=\textbf{(\arabic*)}}
\newlist{Enumerate}{enumerate}{1}
\setlist[Enumerate]{label*=\textbf{(\arabic*)}}

\newlist{Itemize}{itemize}{1}
\setlist[Itemize]{leftmargin=0em,label={}}


\RequirePackage{mycolors}
\RequirePackage{myprogram}
\RequirePackage{mymath}
\RequirePackage{mybiblio}
\RequirePackage{myhyperref}

\RequirePackage{notations}
\RequirePackage{mycomments}
\RequirePackage{myabbreviations}
\RequirePackage{llproof}
\RequirePackage{marvosym} % for \PointingHand / \Hand in llproof
\RequirePackage{thmtools}
\RequirePackage{thm-restate}
\RequirePackage{placeins} % for \FloatBarrier
\RequirePackage{afterpage}
\RequirePackage{mywrapfig}

\RequirePackage{marginnote}
\RequirePackage{mathtools}

%% Clever refs.

\RequirePackage [noabbrev,capitalize,nameinlink]{cleveref}
%% \crefformat{lstnumber}{#2line #1#3}
\newcommand{\Sect}{}\let \Sect \latexS

\crefformat{appendix}{#2\Sect#1#3}
\Crefformat{appendix}{#2\Sect#1#3}
\crefformat{section}{#2\Sect#1#3}
\Crefformat{section}{#2\Sect#1#3}
\crefformat{listing}{#2example~#1#3}
\Crefformat{listing}{#2Example~#1#3}
\crefformat{footnote}{#2\footnotemark[#1]#3}
\Crefformat{footnote}{#2\footnotemark[#1]#3}
%% \crefformat{figure}{#2figure~#1#3}
%% \Crefformat{Figure}{#2Figure~#1#3}

% A manual link to the a footnote. \cref prints something that
% looks like a section reference (not a footnote).
\newcommand{\footnotecref}[1]{\hyperref[#1]{\textsuperscript{\ref*{#1}}}}

\newenvironment{local}{}{\ignorespacesafterend}

\RequirePackage{subcaption}
\RequirePackage{myboxes}


%% \newcommand{\polytype}{boxed polytype\xspace}
%% \newcommand{\polytypes}{boxed polytypes\xspace}
%% \newcommand{\Polytype}{Boxed polytype\xspace}
%% \newcommand{\Polytypes}{Boxed polytypes\xspace}

%% You overrode our changes. Both Alistair and I agreed on explaining
%% polytypes and then keep polytypes.  Boxed polytypes let people thing of
%% Boxy types which is still in use and is something else.


\newcommand{\polytype}{polytype\xspace}
\newcommand{\polytypes}{polytypes\xspace}
\newcommand{\Polytype}{Polytype\xspace}
\newcommand{\Polytypes}{Polytypes\xspace}

\newcommand{\Blacksquare}{{\color{black!60}\blacksquare}}

%% \RequirePackage{needspace}
%% \newcommand{\ifneedspace}[3]{%
%%   \par \penalty-100\begingroup
%%   \setlength{\dimen@}{#1}%
%%   \dimen@ii\pagegoal \advance\dimen@ii-\pagetotal
%%   \ifdim \dimen@>\dimen@ii
%%     #2
%%   \else
%%     #3
%%   \fi\endgroup%
%% }
